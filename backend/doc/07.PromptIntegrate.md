# System Prompt 통합 및 표준화 구현 계획

**작성일**: 2025-01-03
**버전**: 1.1
**목적**: 분산된 Claude prompt를 통합하고 Markdown 표준화

---

## 📋 목표

1. **System Prompt 통합**: 3곳에 분산된 prompt를 1곳으로 통합
2. **Markdown 표준화**: Structured + Markdown 장점 결합
3. **동적 섹션 제목 지원**: Claude가 생성한 제목을 HWP에 반영
4. **빈칸 없는 파싱**: 모든 필드를 정확하게 추출
5. **Topic을 Message로 전달**: System prompt는 순수 지침만, topic은 message에 포함

---

## 📊 현황 분석

### 문제점

| 문제 | 현재 상태 | 영향 |
|------|----------|------|
| **Prompt 중복** | 3곳에 하드코딩 | 유지보수 어려움, 불일치 위험 |
| **Topic in System Prompt** | System prompt에 topic 포함 | 순수성 저해, 재사용성 낮음 |
| **섹션 제목 고정** | `markdown_parser.py`가 기본값만 사용 | Claude 출력 무시 |
| **파싱 실패** | 키워드 매칭 실패 시 빈칸 | HWP 템플릿 치환 누락 |

### 영향 범위

- `backend/app/utils/claude_client.py` (2개 메서드)
- `backend/app/routers/topics.py` (1개 엔드포인트)
- `backend/app/utils/markdown_parser.py` (파싱 로직)

---

## 🎯 핵심 설계 원칙

### 1. System Prompt의 순수성

**System Prompt는 역할과 지침만 포함:**
```python
FINANCIAL_REPORT_SYSTEM_PROMPT = """당신은 금융 기관의 전문 보고서 작성자입니다.
...
섹션 제목을 자유롭게 커스터마이징하세요.
..."""
```

**Topic은 Message로 전달:**
```python
messages = [
    {"role": "user", "content": "대화 주제: 디지털뱅킹 트렌드\n\n이전 메시지를 참고하세요."},
    {"role": "user", "content": "보고서를 작성해주세요."},
    ...
]
```

### 2. 장점

| 항목 | System Prompt에 Topic | Message에 Topic |
|------|----------------------|----------------|
| **재사용성** | ❌ Topic마다 다름 | ✅ 모든 Topic에 동일 |
| **순수성** | ❌ 지침 + 데이터 혼재 | ✅ 순수 지침만 |
| **유연성** | ❌ 함수 필요 | ✅ Message 조립만 |
| **토큰 효율** | ⚠️ System 매번 전송 | ✅ Message는 선택적 |

---

## 🚀 구현 단계

### Phase 1: Prompts 모듈 생성

#### 1.1 `backend/app/utils/prompts.py` 생성

**파일 위치**: `backend/app/utils/prompts.py`

**이유**:
- Utils는 유틸리티 함수/상수를 모아두는 곳
- `claude_client.py`, `markdown_parser.py`와 같은 레벨에 배치
- Config는 설정값(환경변수 등)을 다루는 곳이므로 prompts는 utils가 적합

```python
"""
Claude API System Prompts
보고서 생성에 사용되는 system prompt 상수 정의
"""

# ============================================================
# Financial Report System Prompt (통합 버전)
# ============================================================

FINANCIAL_REPORT_SYSTEM_PROMPT = """당신은 금융 기관의 전문 보고서 작성자입니다.
사용자가 제공하는 주제에 대해 금융 업무보고서를 작성해주세요.

아래 형식에 맞춰 각 섹션을 작성해주세요:

1. **제목** - 간결하고 명확하게
2. **요약 섹션** - 2-3문단으로 핵심 내용 요약
   - 섹션 제목 예: "요약", "핵심 요약", "Executive Summary" 등
3. **배경 섹션** - 왜 이 보고서가 필요한지 설명
   - 섹션 제목 예: "배경 및 목적", "추진 배경", "사업 배경" 등
4. **주요 내용 섹션** - 구체적이고 상세한 분석 및 설명 (3-5개 소제목 포함)
   - 섹션 제목 예: "주요 내용", "분석 결과", "세부 내역" 등
5. **결론 섹션** - 요약과 향후 조치사항
   - 섹션 제목 예: "결론 및 제언", "향후 계획", "시사점" 등

전문적이고 격식있는 문체로 작성하되, 명확하고 이해하기 쉽게 작성해주세요.
금융 용어와 데이터를 적절히 활용하여 신뢰성을 높여주세요.

**출력은 반드시 다음 Markdown 형식을 사용하세요:**
- # {제목} (H1)
- ## {요약 섹션 제목} (H2) - 주제에 맞는 적절한 제목 사용
- ## {배경 섹션 제목} (H2) - 주제에 맞는 적절한 제목 사용
- ## {주요내용 섹션 제목} (H2) - 주제에 맞는 적절한 제목 사용
- ## {결론 섹션 제목} (H2) - 주제에 맞는 적절한 제목 사용

각 섹션 제목은 보고서 내용과 맥락에 맞게 자유롭게 작성하되,
반드시 위의 4개 섹션(요약, 배경, 주요내용, 결론) 순서를 따라야 합니다."""


def create_topic_context_message(topic_input_prompt: str) -> dict:
    """대화 주제를 포함하는 context message를 생성합니다.

    이 함수는 Topics API의 MessageAsk 엔드포인트에서 사용되며,
    대화의 주제를 첫 번째 user message로 추가하여
    Claude가 일관된 맥락을 유지하도록 돕습니다.

    Args:
        topic_input_prompt: 대화 주제 (예: "2025 디지털뱅킹 트렌드 분석")

    Returns:
        Claude API messages 형식의 딕셔너리
        {
            "role": "user",
            "content": "대화 주제: {topic}\\n\\n이전 메시지를 참고하세요."
        }

    Examples:
        >>> msg = create_topic_context_message("디지털뱅킹 트렌드")
        >>> msg["role"]
        'user'
        >>> "디지털뱅킹 트렌드" in msg["content"]
        True
    """
    return {
        "role": "user",
        "content": f"**대화 주제**: {topic_input_prompt}\n\n이전 메시지들을 문맥으로 활용하여 일관된 문체와 구조로 답변하세요."
    }
```

**핵심 포인트:**
- ✅ System prompt는 순수하게 지침만
- ✅ `create_topic_context_message()`: Topic을 user message로 변환
- ✅ `get_topic_contextualized_prompt()` 제거 (더 이상 불필요)

---

### Phase 2: Markdown Parser 개선

#### 2.1 `backend/app/utils/markdown_parser.py` 전체 교체

**기존 문제:**
- 섹션 제목 하드코딩
- 고정 키워드만 인식
- 매칭 실패 시 빈칸

**개선 내용:**
- 모든 H2 섹션 동적 추출
- 키워드 기반 분류 (Robust)
- 실제 제목을 `title_xxx`에 저장

```python
"""
Markdown 파일을 파싱하여 HWPHandler에 필요한 content dict로 변환
"""
import re
from typing import Dict, Tuple, List


def parse_markdown_to_content(md_text: str) -> Dict[str, str]:
    """Markdown 텍스트를 파싱하여 HWPHandler.generate_report()에 필요한 content dict로 변환.

    섹션 제목을 동적으로 추출하여 HWP 템플릿의 {{TITLE_XXX}} 플레이스홀더에 대입합니다.

    Args:
        md_text: Markdown 형식의 텍스트

    Returns:
        HWP 플레이스홀더에 매핑될 content 딕셔너리:
            - title: 보고서 제목
            - summary: 요약 내용
            - background: 배경 내용
            - main_content: 주요 내용
            - conclusion: 결론 내용
            - title_summary: 요약 섹션 제목 (동적 추출)
            - title_background: 배경 섹션 제목 (동적 추출)
            - title_main_content: 주요 내용 섹션 제목 (동적 추출)
            - title_conclusion: 결론 섹션 제목 (동적 추출)

    Examples:
        >>> md = "# 디지털뱅킹 트렌드\\n\\n## 핵심 요약\\n\\n2025년..."
        >>> content = parse_markdown_to_content(md)
        >>> content['title_summary']
        '핵심 요약'
        >>> len(content['summary']) > 0
        True
    """
    content = {
        "title": "",
        "summary": "",
        "background": "",
        "main_content": "",
        "conclusion": "",
        "title_summary": "요약",
        "title_background": "배경 및 목적",
        "title_main_content": "주요 내용",
        "title_conclusion": "결론 및 제언"
    }

    # 1. H1 제목 추출
    title_match = re.search(r'^#\s+(.+)$', md_text, re.MULTILINE)
    if title_match:
        content["title"] = title_match.group(1).strip()

    # 2. 모든 H2 섹션 추출 (제목 + 내용)
    h2_sections = extract_all_h2_sections(md_text)

    # 3. 키워드 기반으로 섹션 분류 및 매핑
    for section_title, section_content in h2_sections:
        section_type = classify_section(section_title)

        if section_type == "summary":
            content["title_summary"] = section_title
            content["summary"] = section_content
        elif section_type == "background":
            content["title_background"] = section_title
            content["background"] = section_content
        elif section_type == "main_content":
            content["title_main_content"] = section_title
            content["main_content"] = section_content
        elif section_type == "conclusion":
            content["title_conclusion"] = section_title
            content["conclusion"] = section_content

    # 4. 내용이 비어있으면 전체 텍스트를 main_content로 사용
    if not any([content["summary"], content["background"],
                content["main_content"], content["conclusion"]]):
        if title_match:
            content["main_content"] = md_text[title_match.end():].strip()
        else:
            content["main_content"] = md_text.strip()

    return content


def extract_all_h2_sections(md_text: str) -> List[Tuple[str, str]]:
    """Markdown에서 모든 H2 섹션을 추출합니다.

    Args:
        md_text: Markdown 텍스트

    Returns:
        (섹션 제목, 섹션 내용) 튜플의 리스트

    Examples:
        >>> md = "# Title\\n\\n## 요약\\n\\nSummary text\\n\\n## 배경\\n\\nBackground text"
        >>> sections = extract_all_h2_sections(md)
        >>> len(sections)
        2
        >>> sections[0]
        ('요약', 'Summary text')
    """
    # H2 헤더와 그 내용을 추출하는 정규식
    # 패턴: ## 제목\n\n내용 (다음 ##까지 또는 끝까지)
    pattern = r'^##\s+(.+?)\s*$\n+(.*?)(?=\n+^##\s|\Z)'
    matches = re.findall(pattern, md_text, re.MULTILINE | re.DOTALL)

    return [(title.strip(), content.strip()) for title, content in matches]


def classify_section(section_title: str) -> str:
    """섹션 제목을 분석하여 종류를 분류합니다.

    키워드 기반으로 섹션이 요약/배경/주요내용/결론 중 어디에 해당하는지 판단합니다.

    Args:
        section_title: H2 섹션 제목

    Returns:
        섹션 타입: "summary", "background", "main_content", "conclusion", "unknown"

    Examples:
        >>> classify_section("핵심 요약")
        'summary'
        >>> classify_section("추진 배경")
        'background'
        >>> classify_section("Executive Summary")
        'summary'
        >>> classify_section("향후 계획 및 제언")
        'conclusion'
    """
    title_lower = section_title.lower()

    # 요약 키워드
    summary_keywords = ["요약", "summary", "핵심", "개요", "executive"]
    if any(keyword in title_lower for keyword in summary_keywords):
        return "summary"

    # 배경 키워드
    background_keywords = ["배경", "목적", "background", "추진", "사업", "필요성", "경위"]
    if any(keyword in title_lower for keyword in background_keywords):
        return "background"

    # 결론 키워드 (주요내용보다 먼저 체크)
    conclusion_keywords = ["결론", "제언", "conclusion", "향후", "계획", "시사점", "recommendation"]
    if any(keyword in title_lower for keyword in conclusion_keywords):
        return "conclusion"

    # 주요내용 키워드
    main_keywords = ["주요", "내용", "분석", "결과", "세부", "상세", "내역", "현황", "main", "detail", "analysis"]
    if any(keyword in title_lower for keyword in main_keywords):
        return "main_content"

    return "unknown"


def extract_title_from_markdown(md_text: str) -> str:
    """Markdown에서 제목만 추출 (간편 함수).

    Args:
        md_text: Markdown 텍스트

    Returns:
        추출된 제목 (없으면 "보고서")

    Examples:
        >>> md = "# 2025 디지털뱅킹 보고서\\n\\n내용..."
        >>> title = extract_title_from_markdown(md)
        >>> title
        '2025 디지털뱅킹 보고서'
    """
    title_match = re.search(r'^#\s+(.+)$', md_text, re.MULTILINE)
    if title_match:
        return title_match.group(1).strip()
    return "보고서"
```

---

### Phase 3: ClaudeClient 수정

#### 3.1 `backend/app/utils/claude_client.py` 수정

**변경 1: Import 추가 (파일 상단, line 11 근처)**
```python
# 기존 imports 뒤에 추가
from app.utils.prompts import FINANCIAL_REPORT_SYSTEM_PROMPT
```

**변경 2: `chat_completion` 메서드 (line 248-260)**

**Before:**
```python
# Default system prompt for financial reports
if system_prompt is None:
    system_prompt = """당신은 금융 기관의 전문 보고서 작성자입니다.
사용자의 요청에 따라 전문적이고 격식있는 금융 업무보고서를 Markdown 형식으로 작성해주세요.
...
모든 출력은 Markdown 형식이어야 합니다."""
```

**After:**
```python
# Default system prompt for financial reports
if system_prompt is None:
    system_prompt = FINANCIAL_REPORT_SYSTEM_PROMPT
```

**변경 3: `generate_report` 메서드**

**Before (line 60-89):**
```python
prompt = f"""당신은 금융 기관의 전문 보고서 작성자입니다.
다음 주제에 대한 금융 업무보고서를 작성해주세요.

주제: {topic}
...
[제목]
[배경제목]
..."""
```

**After:**
```python
user_message = f"다음 주제로 금융 업무보고서를 작성해주세요:\n\n{topic}"
```

**변경 4: `generate_report` API 호출 (line 96-101)**

**Before:**
```python
message = self.client.messages.create(
    model=self.model,
    max_tokens=self.max_tokens,
    messages=[
        {"role": "user", "content": prompt}
    ]
)
```

**After:**
```python
message = self.client.messages.create(
    model=self.model,
    max_tokens=self.max_tokens,
    system=FINANCIAL_REPORT_SYSTEM_PROMPT,
    messages=[
        {"role": "user", "content": user_message}
    ]
)
```

**변경 5: 반환 타입 변경 (Markdown 반환)**

**Before (line 40-58):**
```python
def generate_report(self, topic: str) -> Dict[str, str]:
    """
    주제를 받아 금융 업무보고서 내용을 생성합니다.
    ...
    Returns:
        Dict[str, str]: 보고서 각 섹션의 내용
    """
```

**After:**
```python
def generate_report(self, topic: str) -> str:
    """
    주제를 받아 금융 업무보고서 내용을 Markdown 형식으로 생성합니다.

    Args:
        topic: 보고서 주제

    Returns:
        str: Markdown 형식의 보고서 텍스트

    Examples:
        >>> client = ClaudeClient()
        >>> md_content = client.generate_report("디지털뱅킹 트렌드")
        >>> md_content.startswith("# ")
        True
    """
```

**변경 6: 파싱 로직 제거 (line 104-127)**

**Before:**
```python
# 응답 텍스트 파싱
content = message.content[0].text
...
parsed_content = self._parse_report_content(content)
...
return parsed_content
```

**After:**
```python
# 응답 텍스트 (Markdown)
content = message.content[0].text
...
# Markdown 텍스트 그대로 반환 (파싱은 호출자가 수행)
return content
```

**변경 7: `_parse_report_content` 메서드 삭제 (line 133-209)**

이 메서드는 더 이상 사용하지 않으므로 완전히 삭제합니다.

---

### Phase 4: Topics Router 수정

#### 4.1 `backend/app/routers/topics.py` 수정

**변경 1: Import 추가 (파일 상단)**

```python
from app.utils.prompts import FINANCIAL_REPORT_SYSTEM_PROMPT, create_topic_context_message
from app.utils.markdown_parser import parse_markdown_to_content
```

**변경 2: `create_topic_with_generation` 엔드포인트 (line 117-120)**

**Before:**
```python
claude = ClaudeClient()
result = claude.generate_report(topic_data.input_prompt.strip())
```

**After:**
```python
claude = ClaudeClient()
md_content = claude.generate_report(topic_data.input_prompt.strip())

# Markdown을 파싱하여 content dict로 변환
result = parse_markdown_to_content(md_content)
```

**변경 3: `ask_message` 엔드포인트 - System Prompt 구성 (line 689-708)**

**Before:**
```python
# 시스템 프롬프트 구성
if body.system_prompt:
    system_prompt = body.system_prompt
    logger.info(f"[ASK] Using custom system prompt - length={len(system_prompt)}")
else:
    default_prompt = """당신은 금융 기관의 전문 보고서 작성자입니다.
사용자의 요청에 따라 전문적이고 격식있는 금융 업무보고서를 Markdown 형식으로 작성해주세요.
...
모든 출력은 Markdown 형식이어야 합니다."""

    topic_context = f"\n\n**대화 주제**: {topic.input_prompt}\n이전 메시지를 문맥으로 활용하여 일관된 문체와 구조로 답변하세요."
    system_prompt = default_prompt + topic_context
    logger.info(f"[ASK] Using default system prompt with topic context")
```

**After:**
```python
# 시스템 프롬프트 구성 (순수 지침만)
if body.system_prompt:
    system_prompt = body.system_prompt
    logger.info(f"[ASK] Using custom system prompt - length={len(system_prompt)}")
else:
    system_prompt = FINANCIAL_REPORT_SYSTEM_PROMPT
    logger.info(f"[ASK] Using default system prompt")
```

**변경 4: Topic Context를 Message로 추가 (line 667-671 수정)**

**Before:**
```python
# Claude 메시지 배열 변환
claude_messages = [
    {"role": m.role.value, "content": m.content}
    for m in context_messages
]
```

**After:**
```python
# Claude 메시지 배열 변환
claude_messages = [
    {"role": m.role.value, "content": m.content}
    for m in context_messages
]

# Topic context를 첫 번째 메시지로 추가
topic_context_msg = create_topic_context_message(topic.input_prompt)
claude_messages = [topic_context_msg] + claude_messages

logger.info(f"[ASK] Added topic context as first message - topic={topic.input_prompt}")
```

---

### Phase 5: 호출처 수정

#### 5.1 `backend/app/main.py` 수정 (line 207-210)

**Before:**
```python
content = claude_client.generate_report(request.topic)
logger.info("보고서 내용 생성 완료")

# HWP 파일 생성
```

**After:**
```python
md_content = claude_client.generate_report(request.topic)
logger.info("보고서 내용 생성 완료 (Markdown)")

# Markdown을 파싱하여 content dict로 변환
from app.utils.markdown_parser import parse_markdown_to_content
content = parse_markdown_to_content(md_content)
logger.info("Markdown 파싱 완료")

# HWP 파일 생성
```

#### 5.2 `backend/app/routers/artifacts.py`

→ 변경 불필요 (이미 `markdown_parser` 사용 중)

---

### Phase 6: 검증 및 테스트

#### 6.1 Grep으로 하드코딩된 Prompt 제거 확인

```bash
cd backend
grep -r "당신은 금융 기관의 전문 보고서 작성자입니다" app/ --include="*.py"
```

**기대 결과:** `utils/prompts.py`만 표시

#### 6.2 Import 확인

```bash
cd backend
grep -r "from app.utils.prompts import" app/ --include="*.py"
```

**기대 결과:**
```
app/utils/claude_client.py:from app.utils.prompts import FINANCIAL_REPORT_SYSTEM_PROMPT
app/routers/topics.py:from app.utils.prompts import FINANCIAL_REPORT_SYSTEM_PROMPT, create_topic_context_message
```

#### 6.3 기능 테스트

**테스트 1: Topic Context Message 생성**
```python
from app.utils.prompts import create_topic_context_message

msg = create_topic_context_message("디지털뱅킹 트렌드")

print("=== Topic Context Message ===")
print(f"Role: {msg['role']}")
print(f"Content: {msg['content']}")

# 기대 결과:
# Role: user
# Content: **대화 주제**: 디지털뱅킹 트렌드
#
# 이전 메시지들을 문맥으로 활용하여 일관된 문체와 구조로 답변하세요.
```

**테스트 2: Messages 구성**
```python
from app.utils.prompts import FINANCIAL_REPORT_SYSTEM_PROMPT, create_topic_context_message

topic_msg = create_topic_context_message("디지털뱅킹 트렌드")
user_messages = [
    {"role": "user", "content": "보고서를 작성해주세요."},
    {"role": "assistant", "content": "네, 작성하겠습니다."},
    {"role": "user", "content": "주요 내용을 더 상세히 써주세요."}
]

claude_messages = [topic_msg] + user_messages

print("=== Claude Messages ===")
print(f"Total messages: {len(claude_messages)}")
print(f"First message (topic context): {claude_messages[0]['content'][:50]}...")
print(f"\nSystem Prompt: {FINANCIAL_REPORT_SYSTEM_PROMPT[:100]}...")

# 기대 결과:
# Total messages: 4
# First message (topic context): **대화 주제**: 디지털뱅킹 트렌드...
# System Prompt: 당신은 금융 기관의 전문 보고서 작성자입니다...
```

**테스트 3: End-to-End Message Flow**
```python
from app.utils.claude_client import ClaudeClient
from app.utils.prompts import FINANCIAL_REPORT_SYSTEM_PROMPT, create_topic_context_message

client = ClaudeClient()

# Topic context + user message
topic_msg = create_topic_context_message("2025 디지털뱅킹 트렌드")
messages = [
    topic_msg,
    {"role": "user", "content": "보고서를 작성해주세요."}
]

response, input_tokens, output_tokens = client.chat_completion(
    messages,
    FINANCIAL_REPORT_SYSTEM_PROMPT
)

print("=== Response ===")
print(response[:200])
print(f"\nTokens - Input: {input_tokens}, Output: {output_tokens}")
```

#### 6.4 Markdown 파싱 테스트

```python
from app.utils.markdown_parser import parse_markdown_to_content

md = """# 2025 디지털뱅킹 보고서

## 핵심 요약
디지털 전환이 가속화되고 있습니다.

## 추진 배경
금융권의 변화가 필요합니다.

## 주요 분석 결과
모바일 뱅킹이 성장하고 있습니다.

## 향후 계획
AI 도입을 추진합니다.
"""

content = parse_markdown_to_content(md)

print("=== Parsed Content ===")
print(f"Title: {content['title']}")
print(f"Summary Title: {content['title_summary']}")
print(f"Summary: {content['summary'][:30]}...")
print(f"Background Title: {content['title_background']}")
print(f"Background: {content['background'][:30]}...")
print(f"Main Content Title: {content['title_main_content']}")
print(f"Conclusion Title: {content['title_conclusion']}")

# 기대 결과:
# Title: 2025 디지털뱅킹 보고서
# Summary Title: 핵심 요약
# Summary: 디지털 전환이 가속화되고 있습니다.
# Background Title: 추진 배경
# Background: 금융권의 변화가 필요합니다.
# Main Content Title: 주요 분석 결과
# Conclusion Title: 향후 계획
```

---

## 📦 최종 파일 구조

```
backend/
├── app/
│   ├── routers/
│   │   └── topics.py                    # 🔧 수정 (import, logic)
│   └── utils/
│       ├── prompts.py                   # ✨ 새로 생성 (통합 prompt)
│       ├── claude_client.py             # 🔧 수정 (import, Markdown 반환)
│       └── markdown_parser.py           # 🔧 전체 교체 (동적 제목 추출)
└── main.py                              # 🔧 수정 (파싱 로직 추가)
```

**변경 요약:**
- ✨ `utils/prompts.py` 새로 생성
- 🔧 `utils/claude_client.py` 수정
- 🔧 `utils/markdown_parser.py` 전체 교체
- 🔧 `routers/topics.py` 수정
- 🔧 `main.py` 수정

---

## 🎯 구조 비교

### Before: Topic in System Prompt

```python
# System Prompt (Topic 포함)
system_prompt = f"""당신은 금융 기관의 전문 보고서 작성자입니다.
...
대화 주제: {topic.input_prompt}
이전 메시지를 참고하세요."""

# Messages
messages = [
    {"role": "user", "content": "보고서 작성해주세요."}
]

# API Call
client.messages.create(
    system=system_prompt,
    messages=messages
)
```

**문제점:**
- ❌ Topic마다 다른 system prompt
- ❌ 재사용 불가능
- ❌ 순수성 저해

### After: Topic in Messages

```python
# System Prompt (순수 지침)
system_prompt = FINANCIAL_REPORT_SYSTEM_PROMPT  # 모든 요청에 동일

# Messages (Topic 포함)
messages = [
    {"role": "user", "content": "**대화 주제**: 디지털뱅킹 트렌드\n\n이전 메시지를 참고하세요."},
    {"role": "user", "content": "보고서 작성해주세요."}
]

# API Call
client.messages.create(
    system=system_prompt,
    messages=messages
)
```

**장점:**
- ✅ System prompt 재사용 가능
- ✅ 순수 지침만 포함
- ✅ Message로 유연한 context 전달

---

## 🎯 기대 효과

| 항목 | Before | After |
|------|--------|-------|
| **Prompt 관리** | 3곳 분산 | 1곳 통합 ✅ |
| **System Prompt 순수성** | Topic 혼재 | 순수 지침만 ✅ |
| **재사용성** | 낮음 | 높음 ✅ |
| **섹션 제목** | 하드코딩 | 동적 추출 ✅ |
| **파싱 성공률** | ~70% | ~95% ✅ |
| **코드 중복** | 60줄 | 0줄 ✅ |
| **유지보수성** | 낮음 | 높음 ✅ |

---

## ⚠️ 주의사항

1. **하위 호환성**: 기존 API 동작 완전 동일
2. **Custom prompt**: 사용자 정의 system_prompt 계속 지원
3. **Message 순서**: Topic context가 항상 첫 번째 message
4. **토큰 사용량**: 약간 증가 가능 (topic이 message에 포함)

---

## 🚀 실행 순서

1. **Phase 1**: `utils/prompts.py` 생성
2. **Phase 2**: `markdown_parser.py` 전체 교체
3. **Phase 3**: `claude_client.py` 수정
4. **Phase 4**: `topics.py` 수정 (Topic을 message로)
5. **Phase 5**: `main.py` 수정
6. **Phase 6**: 검증 및 테스트

---

## 📝 구현 체크리스트

- [ ] **Phase 1: Prompts 모듈**
  - [ ] `backend/app/utils/prompts.py` 생성
  - [ ] `FINANCIAL_REPORT_SYSTEM_PROMPT` 정의
  - [ ] `create_topic_context_message()` 함수 구현

- [ ] **Phase 2: Markdown Parser**
  - [ ] `backend/app/utils/markdown_parser.py` 전체 교체
  - [ ] `parse_markdown_to_content()` 개선
  - [ ] `extract_all_h2_sections()` 구현
  - [ ] `classify_section()` 구현

- [ ] **Phase 3: Claude Client**
  - [ ] `backend/app/utils/claude_client.py` 수정
  - [ ] Import 추가 (`from app.utils.prompts import`)
  - [ ] `chat_completion()` system prompt 변경
  - [ ] `generate_report()` Markdown 반환으로 변경
  - [ ] `_parse_report_content()` 삭제

- [ ] **Phase 4: Topics Router**
  - [ ] `backend/app/routers/topics.py` 수정
  - [ ] Import 추가 (`from app.utils.prompts import`)
  - [ ] `create_topic_with_generation` 파싱 로직 추가
  - [ ] `ask_message` system prompt 단순화
  - [ ] `ask_message` topic context를 message로 추가

- [ ] **Phase 5: Main**
  - [ ] `backend/app/main.py` 수정
  - [ ] `generate_report` 파싱 로직 추가

- [ ] **Phase 6: 검증**
  - [ ] Grep으로 하드코딩 제거 확인
  - [ ] Import 확인
  - [ ] Topic context message 테스트
  - [ ] Markdown 파싱 테스트
  - [ ] End-to-End 테스트

---

**문서 작성일**: 2025-01-03
**최종 수정일**: 2025-01-03
**버전**: 1.1
